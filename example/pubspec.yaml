name: cascadia_example
description: A minimal example app demonstrating Cascadia selector usage.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cascadia:
    path: ../
  html: ^0.15.6
